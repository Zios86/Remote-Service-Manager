@AGENTS.md

# Claude Code notes

- Use `PROJECT_MAP.md` for navigation instead of reconstructing the architecture from chat history.
- Keep this file concise. Put procedures in `docs/` and update the map when modules or data flows change.
- On Windows, use the import above instead of a symlink so the repository works without Developer Mode or administrator rights.
